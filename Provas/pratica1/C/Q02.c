#include <stdio.h>
int luta(int *m, int *l, int *rod ){
    
}
int main(){
    int r;
    scanf("%d", &r);
    int m[r];
    int l[r];
    while(r!=0){
        for(int i=0; i<r; i++){
            scanf("%d", m[i]);
        }
        for(int i=0; i<r; i++){
            scanf("%d", l[i]);
        }
    } 
    return 0;
}